-# frozen_string_literal: true
= render(Lists::Index::Component.new(user: current_user, list: @list, limit: nil, editing: true))

/h1 Editing list
/
/== render 'form'
/
/=> link_to 'Show', @list
/'|
/=< link_to 'Back', lists_path

